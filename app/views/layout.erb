<!DOCTYPE HTML>

<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <link rel = "stylesheet" type = "text/css" href = "/stylesheets/main.css">
        <title>Venue Manager</title>
    </head>
    <body>
        <section class = "wrapper">
            <nav>
                <% if is_logged_in?(session)%>
                    <a href = '/home'>Home</a> |
                    <% if !current_user(session).venues.empty? %> 
                        <a href = '/venues'>Manage Venues</a> | <a href = '/bookings'>Bookings</a> | <a href = '/tenants'>Tenants</a> | <a href = '/employees'>Employees</a>
                    <% end %>
                    <a href = '/logout'>Logout</a>
                <% else %>
                    <a href = '/home'>Home</a>
                <% end %>
            </nav>
            <section class = "content">
            <% if flash[:error]%>
                <%= flash[:error]%>
            <% end %>
            <%= yield %>
            </section>
        </section>
    </body>
</html>